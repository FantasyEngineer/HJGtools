<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="20dp">

        <EditText
            android:id="@+id/etInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="书写加密数据"
            android:text="你好我是加密数据"
            android:padding="10dp"
            app:layout_constraintTop_toTopOf="parent">

        </EditText>

        <Button
            android:id="@+id/btnAESEncryption"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginRight="10dp"
            android:onClick="AES"
            android:text="AES加密"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@+id/btnAESDecryption"
            app:layout_constraintTop_toBottomOf="@+id/etInput"></Button>

        <Button
            android:id="@+id/btnAESDecryption"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginLeft="10dp"
            android:onClick="AES"
            android:text="AES解密"
            app:layout_constraintLeft_toRightOf="@+id/btnAESEncryption"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/etInput"></Button>

        <TextView
            android:id="@+id/tvContentEn"
            style="@style/TvContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@+id/btnAESDecryption">

        </TextView>

        <TextView
            android:id="@+id/tvContentDe"
            style="@style/TvContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            app:layout_constraintTop_toBottomOf="@+id/tvContentEn">

        </TextView>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>